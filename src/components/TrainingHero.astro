---
// Componente Hero para página de Ejemplos de Formación
---

<section class="training-hero">
	<div class="hero-content">
		<div class="hero-text">
			<span class="hero-label">Casos de Éxito</span>
			<h1>
				Ejemplos Reales de <span class="highlight">Formación Digital</span>
			</h1>
			<p class="hero-description">
				Descubre cómo organizaciones de todo el mundo están transformando
				la educación y el desarrollo profesional con herramientas digitales
				como LMS y CMS.
			</p>
			<div class="hero-stats">
				<div class="stat-item">
					<span class="stat-number">500+</span>
					<span class="stat-label">Casos de éxito</span>
				</div>
				<div class="stat-item">
					<span class="stat-number">85%</span>
					<span class="stat-label">Mejora en retención</span>
				</div>
				<div class="stat-item">
					<span class="stat-number">60%</span>
					<span class="stat-label">Reducción de costes</span>
				</div>
			</div>
		</div>
		<div class="hero-visual">
			<div class="visual-card card-1">
				<span class="card-icon">🎓</span>
				<p>Universidad Online</p>
			</div>
			<div class="visual-card card-2">
				<span class="card-icon">💼</span>
				<p>Formación Empresas</p>
			</div>
			<div class="visual-card card-3">
				<span class="card-icon">🏫</span>
				<p>Educación K-12</p>
			</div>
			<div class="visual-card card-4">
				<span class="card-icon">🌐</span>
				<p>E-learning Global</p>
			</div>
		</div>
	</div>
</section>

<style>
	.training-hero {
		padding: 8rem 2rem 6rem;
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		position: relative;
		overflow: hidden;
	}

	.training-hero::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background:
			radial-gradient(circle at 20% 50%, rgba(255, 255, 255, 0.1) 0%, transparent 50%),
			radial-gradient(circle at 80% 80%, rgba(255, 255, 255, 0.1) 0%, transparent 50%);
		pointer-events: none;
	}

	.hero-content {
		max-width: 1400px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 4rem;
		align-items: center;
		position: relative;
		z-index: 1;
	}

	.hero-label {
		display: inline-block;
		font-size: 0.9rem;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 2px;
		color: rgba(255, 255, 255, 0.9);
		margin-bottom: 1rem;
		padding: 0.5rem 1.5rem;
		background: rgba(255, 255, 255, 0.2);
		border-radius: 50px;
		backdrop-filter: blur(10px);
	}

	.hero-text h1 {
		font-size: 3.5rem;
		font-weight: 900;
		color: white;
		line-height: 1.1;
		margin: 0 0 1.5rem 0;
	}

	.highlight {
		background: linear-gradient(120deg, rgba(255, 255, 255, 0.3) 0%, rgba(255, 255, 255, 0.1) 100%);
		padding: 0 0.5rem;
		border-radius: 8px;
	}

	.hero-description {
		font-size: 1.25rem;
		line-height: 1.7;
		color: rgba(255, 255, 255, 0.95);
		margin: 0 0 2.5rem 0;
	}

	.hero-stats {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2rem;
		margin-top: 3rem;
	}

	.stat-item {
		text-align: center;
		padding: 1.5rem;
		background: rgba(255, 255, 255, 0.15);
		border-radius: 16px;
		backdrop-filter: blur(10px);
		border: 1px solid rgba(255, 255, 255, 0.2);
	}

	.stat-number {
		display: block;
		font-size: 2.5rem;
		font-weight: 900;
		color: white;
		margin-bottom: 0.5rem;
	}

	.stat-label {
		display: block;
		font-size: 0.9rem;
		color: rgba(255, 255, 255, 0.9);
	}

	.hero-visual {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 1.5rem;
		position: relative;
	}

	.visual-card {
		background: rgba(255, 255, 255, 0.95);
		padding: 2rem;
		border-radius: 20px;
		text-align: center;
		transition: all 0.3s ease;
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
	}

	.visual-card:hover {
		transform: translateY(-10px);
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
	}

	.card-1 {
		animation: float 3s ease-in-out infinite;
	}

	.card-2 {
		animation: float 3s ease-in-out infinite 0.5s;
	}

	.card-3 {
		animation: float 3s ease-in-out infinite 1s;
	}

	.card-4 {
		animation: float 3s ease-in-out infinite 1.5s;
	}

	.card-icon {
		font-size: 3rem;
		display: block;
		margin-bottom: 1rem;
	}

	.visual-card p {
		margin: 0;
		font-weight: 700;
		color: #667eea;
		font-size: 1rem;
	}

	@keyframes float {
		0%, 100% {
			transform: translateY(0);
		}
		50% {
			transform: translateY(-10px);
		}
	}

	@media (max-width: 1024px) {
		.hero-content {
			grid-template-columns: 1fr;
			gap: 3rem;
		}

		.hero-text h1 {
			font-size: 2.5rem;
		}

		.hero-stats {
			grid-template-columns: repeat(3, 1fr);
			gap: 1rem;
		}
	}

	@media (max-width: 768px) {
		.training-hero {
			padding: 6rem 1.5rem 4rem;
		}

		.hero-text h1 {
			font-size: 2rem;
		}

		.hero-description {
			font-size: 1.1rem;
		}

		.hero-stats {
			grid-template-columns: 1fr;
			gap: 1rem;
		}

		.hero-visual {
			grid-template-columns: 1fr;
		}
	}
</style>
