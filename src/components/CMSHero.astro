---
// CMS Hero Component - Sección introductoria elegante para CMS
// Creado por: Eduardo
---

<section class="cms-hero">
	<div class="hero-content">
		<div class="hero-badge">
			<span class="badge-icon">📝</span>
			<span class="badge-text">Content Management System</span>
		</div>
		<h1 class="hero-title">¿Qué es un <span class="gradient-text">CMS</span>?</h1>
		<p class="hero-description">
			Un Sistema de Gestión de Contenidos (CMS) es una aplicación que permite crear, gestionar y 
			publicar contenido digital de forma sencilla, sin necesidad de conocimientos técnicos avanzados.
		</p>
		<div class="hero-stats">
			<div class="stat-item">
				<span class="stat-number">43%</span>
				<span class="stat-label">de sitios usan WordPress</span>
			</div>
			<div class="stat-divider"></div>
			<div class="stat-item">
				<span class="stat-number">+800M</span>
				<span class="stat-label">sitios web con CMS</span>
			</div>
			<div class="stat-divider"></div>
			<div class="stat-item">
				<span class="stat-number">2026</span>
				<span class="stat-label">transformación digital</span>
			</div>
		</div>
	</div>
	<div class="hero-visual">
		<div class="visual-card">
			<div class="card-header">
				<div class="card-dots">
					<span></span><span></span><span></span>
				</div>
				<span class="card-title">Panel de Administración CMS</span>
			</div>
			<div class="card-content">
				<div class="content-item">
					<div class="content-icon">📄</div>
					<div class="content-info">
						<span class="content-name">Nueva entrada de blog</span>
						<span class="content-status status-draft">Borrador</span>
					</div>
					<span class="content-action">✏️</span>
				</div>
				<div class="content-item">
					<div class="content-icon">🖼️</div>
					<div class="content-info">
						<span class="content-name">Galería de imágenes</span>
						<span class="content-status status-published">Publicado</span>
					</div>
					<span class="content-action">👁️</span>
				</div>
				<div class="content-item">
					<div class="content-icon">🎬</div>
					<div class="content-info">
						<span class="content-name">Video tutorial</span>
						<span class="content-status status-published">Publicado</span>
					</div>
					<span class="content-action">👁️</span>
				</div>
				<div class="content-item">
					<div class="content-icon">📰</div>
					<div class="content-info">
						<span class="content-name">Página de contacto</span>
						<span class="content-status status-scheduled">Programado</span>
					</div>
					<span class="content-action">⏰</span>
				</div>
			</div>
			<div class="card-footer">
				<button class="btn-primary">Crear contenido</button>
				<button class="btn-secondary">Ver estadísticas</button>
			</div>
		</div>
		<div class="floating-elements">
			<div class="floating-badge badge-1">🚀 Fácil de usar</div>
			<div class="floating-badge badge-2">⚡ Rápido</div>
			<div class="floating-badge badge-3">🔧 Personalizable</div>
		</div>
	</div>
</section>

<style>
	.cms-hero {
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		min-height: 90vh;
		display: flex;
		align-items: center;
		padding: 4rem 2rem;
		position: relative;
		overflow: hidden;
	}

	.cms-hero::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: 
			radial-gradient(circle at 20% 50%, rgba(255, 255, 255, 0.1) 0%, transparent 50%),
			radial-gradient(circle at 80% 80%, rgba(255, 255, 255, 0.1) 0%, transparent 50%);
		pointer-events: none;
	}

	.hero-content {
		max-width: 1400px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 4rem;
		align-items: center;
		position: relative;
		z-index: 1;
		width: 100%;
	}

	.hero-badge {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		background: rgba(255, 255, 255, 0.2);
		backdrop-filter: blur(10px);
		padding: 0.75rem 1.5rem;
		border-radius: 50px;
		border: 1px solid rgba(255, 255, 255, 0.3);
		margin-bottom: 2rem;
	}

	.badge-icon {
		font-size: 1.2rem;
	}

	.badge-text {
		color: white;
		font-weight: 600;
		font-size: 0.95rem;
		letter-spacing: 0.5px;
	}

	.hero-title {
		font-size: 4rem;
		font-weight: 900;
		color: white;
		margin-bottom: 1.5rem;
		line-height: 1.1;
		letter-spacing: -1px;
	}

	.gradient-text {
		background: linear-gradient(135deg, #fff 0%, #f0e5ff 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.hero-description {
		font-size: 1.25rem;
		color: rgba(255, 255, 255, 0.9);
		line-height: 1.8;
		margin-bottom: 3rem;
		max-width: 600px;
	}

	.hero-stats {
		display: flex;
		gap: 2rem;
		align-items: center;
		margin-bottom: 2rem;
	}

	.stat-item {
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
	}

	.stat-number {
		font-size: 2rem;
		font-weight: 800;
		color: white;
	}

	.stat-label {
		font-size: 0.85rem;
		color: rgba(255, 255, 255, 0.8);
	}

	.stat-divider {
		width: 1px;
		height: 40px;
		background: rgba(255, 255, 255, 0.3);
	}

	.hero-author {
		display: inline-flex;
		align-items: center;
		gap: 0.75rem;
		background: rgba(255, 255, 255, 0.15);
		backdrop-filter: blur(10px);
		padding: 0.75rem 1.5rem;
		border-radius: 50px;
		border: 1px solid rgba(255, 255, 255, 0.2);
	}

	.author-label {
		color: rgba(255, 255, 255, 0.8);
		font-size: 0.9rem;
	}

	.author-name {
		color: white;
		font-weight: 700;
		font-size: 1rem;
	}

	.hero-visual {
		position: relative;
	}

	.visual-card {
		background: white;
		border-radius: 24px;
		padding: 2rem;
		box-shadow: 
			0 20px 60px rgba(0, 0, 0, 0.3),
			0 0 0 1px rgba(255, 255, 255, 0.1) inset;
		position: relative;
		z-index: 2;
	}

	.card-header {
		display: flex;
		align-items: center;
		gap: 1rem;
		margin-bottom: 1.5rem;
		padding-bottom: 1rem;
		border-bottom: 1px solid #e5e7eb;
	}

	.card-dots {
		display: flex;
		gap: 0.5rem;
	}

	.card-dots span {
		width: 12px;
		height: 12px;
		border-radius: 50%;
		background: #cbd5e1;
	}

	.card-dots span:nth-child(1) { background: #ef4444; }
	.card-dots span:nth-child(2) { background: #f59e0b; }
	.card-dots span:nth-child(3) { background: #10b981; }

	.card-title {
		font-weight: 700;
		color: #1f2937;
		font-size: 1rem;
	}

	.card-content {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		margin-bottom: 1.5rem;
	}

	.content-item {
		display: flex;
		align-items: center;
		gap: 1rem;
		padding: 1rem;
		background: #f9fafb;
		border-radius: 12px;
		transition: all 0.3s ease;
		cursor: pointer;
	}

	.content-item:hover {
		background: #f3f4f6;
		transform: translateX(4px);
	}

	.content-icon {
		font-size: 1.5rem;
		width: 40px;
		height: 40px;
		display: flex;
		align-items: center;
		justify-content: center;
		background: white;
		border-radius: 10px;
	}

	.content-info {
		flex: 1;
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
	}

	.content-name {
		font-weight: 600;
		color: #1f2937;
		font-size: 0.95rem;
	}

	.content-status {
		font-size: 0.75rem;
		font-weight: 600;
		padding: 0.25rem 0.75rem;
		border-radius: 12px;
		display: inline-block;
		width: fit-content;
	}

	.status-draft {
		background: #fef3c7;
		color: #92400e;
	}

	.status-published {
		background: #d1fae5;
		color: #065f46;
	}

	.status-scheduled {
		background: #dbeafe;
		color: #1e40af;
	}

	.content-action {
		font-size: 1.2rem;
		opacity: 0.5;
		transition: opacity 0.3s ease;
	}

	.content-item:hover .content-action {
		opacity: 1;
	}

	.card-footer {
		display: flex;
		gap: 1rem;
	}

	.btn-primary, .btn-secondary {
		flex: 1;
		padding: 0.875rem 1.5rem;
		border-radius: 12px;
		font-weight: 600;
		font-size: 0.95rem;
		border: none;
		cursor: pointer;
		transition: all 0.3s ease;
	}

	.btn-primary {
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		color: white;
	}

	.btn-primary:hover {
		transform: translateY(-2px);
		box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
	}

	.btn-secondary {
		background: white;
		color: #667eea;
		border: 2px solid #667eea;
	}

	.btn-secondary:hover {
		background: #f9fafb;
	}

	.floating-elements {
		position: absolute;
		width: 100%;
		height: 100%;
		pointer-events: none;
	}

	.floating-badge {
		position: absolute;
		background: white;
		padding: 0.75rem 1.25rem;
		border-radius: 50px;
		font-weight: 600;
		font-size: 0.9rem;
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
		animation: float 3s ease-in-out infinite;
	}

	.badge-1 {
		top: 10%;
		right: -10%;
		color: #667eea;
		animation-delay: 0s;
	}

	.badge-2 {
		bottom: 30%;
		right: -5%;
		color: #764ba2;
		animation-delay: 1s;
	}

	.badge-3 {
		top: 50%;
		right: -15%;
		color: #9f7aea;
		animation-delay: 2s;
	}

	@keyframes float {
		0%, 100% {
			transform: translateY(0px);
		}
		50% {
			transform: translateY(-20px);
		}
	}

	@media (max-width: 968px) {
		.cms-hero {
			min-height: auto;
			padding: 3rem 1.5rem;
		}

		.hero-content {
			grid-template-columns: 1fr;
			gap: 3rem;
		}

		.hero-title {
			font-size: 2.5rem;
		}

		.hero-description {
			font-size: 1.1rem;
		}

		.hero-stats {
			flex-wrap: wrap;
			gap: 1.5rem;
		}

		.stat-number {
			font-size: 1.5rem;
		}

		.floating-badge {
			display: none;
		}

		.card-footer {
			flex-direction: column;
		}
	}
</style>
