---
---

<section class="hero">
	<div class="hero-bg">
		<div class="orb orb-1"></div>
		<div class="orb orb-2"></div>
		<div class="orb orb-3"></div>
	</div>
	<div class="hero-content">
		<span class="section-label">WordPress como CMS</span>
		<h1>Usos habituales de WordPress</h1>
		<p>
			WordPress es el CMS más usado del mundo porque permite crear sitios rápidos,
			escalables y fáciles de administrar. Aquí tienes los usos más comunes y cómo
			aporta valor en cada caso.
		</p>
		<div class="hero-actions">
			<a href="/seguridad-wordpress" class="btn btn-primary">Ver seguridad</a>
			<a href="/que-es-lms" class="btn btn-secondary">¿Qué es un LMS?</a>
		</div>
	</div>
</section>

<style>
	.hero {
		position: relative;
		padding: 7rem 2rem 5rem;
		text-align: center;
		overflow: hidden;
	}

	.hero-bg {
		position: absolute;
		inset: 0;
		pointer-events: none;
	}

	.orb {
		position: absolute;
		border-radius: 50%;
		filter: blur(80px);
		opacity: 0.5;
	}

	.orb-1 {
		width: 500px;
		height: 500px;
		background: linear-gradient(135deg, rgba(99, 102, 241, 0.25), rgba(139, 92, 246, 0.2));
		top: -180px;
		right: -120px;
	}

	.orb-2 {
		width: 360px;
		height: 360px;
		background: linear-gradient(135deg, rgba(6, 182, 212, 0.18), rgba(99, 102, 241, 0.15));
		bottom: -120px;
		left: -120px;
	}

	.orb-3 {
		width: 240px;
		height: 240px;
		background: linear-gradient(135deg, rgba(139, 92, 246, 0.2), rgba(236, 72, 153, 0.12));
		top: 45%;
		left: 55%;
		transform: translate(-50%, -50%);
	}

	.hero-content {
		position: relative;
		z-index: 1;
		max-width: 820px;
		margin: 0 auto;
	}

	.hero h1 {
		font-size: 3.25rem;
		margin: 1rem 0 1.25rem;
		letter-spacing: -1px;
	}

	.hero p {
		font-size: 1.2rem;
		margin: 0 auto 2rem;
		max-width: 700px;
	}

	.section-label {
		display: inline-block;
		font-size: 0.85rem;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 2px;
		color: var(--primary-color);
	}

	.hero-actions {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	.btn {
		padding: 0.9rem 1.8rem;
		border-radius: var(--radius-full);
		text-decoration: none;
		font-weight: 600;
		font-size: 0.95rem;
		transition: all var(--transition-base);
		display: inline-flex;
		align-items: center;
		justify-content: center;
		border: none;
	}

	.btn-primary {
		background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
		color: white;
		box-shadow: 0 4px 15px rgba(99, 102, 241, 0.35);
	}

	.btn-primary:hover {
		transform: translateY(-2px);
		box-shadow: 0 8px 25px rgba(99, 102, 241, 0.45);
		color: white;
	}

	.btn-secondary {
		background: var(--surface-color);
		color: var(--text-primary);
		border: 1px solid var(--border-color);
	}

	.btn-secondary:hover {
		background: var(--bg-secondary);
		border-color: var(--primary-light);
		color: var(--primary-color);
		transform: translateY(-2px);
	}

	@media (max-width: 768px) {
		.hero h1 {
			font-size: 2.4rem;
		}
	}
</style>
