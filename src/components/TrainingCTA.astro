---
// Componente Call to Action para página de Formación
---

<section class="training-cta">
	<div class="cta-content">
		<div class="cta-text">
			<span class="cta-label">¿Listo para empezar?</span>
			<h2>Transforma tu Formación con Tecnología Digital</h2>
			<p>
				Únete a miles de organizaciones que ya están revolucionando
				la educación y el desarrollo profesional con herramientas digitales.
				Comienza tu transformación hoy mismo.
			</p>
			<div class="cta-stats">
				<div class="stat">
					<span class="stat-emoji">🚀</span>
					<div>
						<strong>Implementación rápida</strong>
						<p>En menos de 2 semanas</p>
					</div>
				</div>
				<div class="stat">
					<span class="stat-emoji">💰</span>
					<div>
						<strong>ROI comprobado</strong>
						<p>Ahorro del 60% en costes</p>
					</div>
				</div>
				<div class="stat">
					<span class="stat-emoji">📈</span>
					<div>
						<strong>Mejores resultados</strong>
						<p>+85% retención de aprendizaje</p>
					</div>
				</div>
			</div>
		</div>

		<div class="cta-actions">
			<div class="action-card primary">
				<h3>Explora Plataformas LMS</h3>
				<p>Descubre las mejores soluciones para gestionar formación online</p>
				<a href="/que-es-lms" class="cta-button primary">
					<span>Ver Plataformas LMS</span>
					<span class="arrow">→</span>
				</a>
			</div>

			<div class="action-card secondary">
				<h3>WordPress como LMS</h3>
				<p>Convierte tu sitio WordPress en una plataforma de formación completa</p>
				<a href="/wordpress-lms" class="cta-button secondary">
					<span>Descubre WordPress LMS</span>
					<span class="arrow">→</span>
				</a>
			</div>

			<div class="action-card tertiary">
				<h3>Sistemas de Gestión</h3>
				<p>Aprende sobre CMS y cómo gestionar contenido educativo</p>
				<a href="/que-es-cms" class="cta-button tertiary">
					<span>Explorar CMS</span>
					<span class="arrow">→</span>
				</a>
			</div>
		</div>
	</div>

	<div class="cta-footer">
		<p>¿Necesitas asesoramiento personalizado? <a href="#contacto">Contacta con nuestro equipo</a></p>
	</div>
</section>

<style>
	.training-cta {
		padding: 6rem 2rem;
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		position: relative;
		overflow: hidden;
	}

	.training-cta::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background:
			radial-gradient(circle at 30% 50%, rgba(255, 255, 255, 0.1) 0%, transparent 50%),
			radial-gradient(circle at 70% 80%, rgba(255, 255, 255, 0.1) 0%, transparent 50%);
		pointer-events: none;
	}

	.cta-content {
		max-width: 1400px;
		margin: 0 auto;
		position: relative;
		z-index: 1;
	}

	.cta-text {
		text-align: center;
		margin-bottom: 4rem;
		color: white;
	}

	.cta-label {
		display: inline-block;
		font-size: 0.9rem;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 2px;
		color: rgba(255, 255, 255, 0.9);
		margin-bottom: 1rem;
		padding: 0.5rem 1.5rem;
		background: rgba(255, 255, 255, 0.2);
		border-radius: 50px;
		backdrop-filter: blur(10px);
	}

	.cta-text h2 {
		font-size: 3rem;
		font-weight: 900;
		margin: 0 0 1.5rem 0;
		line-height: 1.2;
	}

	.cta-text > p {
		font-size: 1.2rem;
		line-height: 1.7;
		max-width: 800px;
		margin: 0 auto 3rem;
		opacity: 0.95;
	}

	.cta-stats {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2rem;
		max-width: 900px;
		margin: 0 auto;
	}

	.stat {
		display: flex;
		align-items: center;
		gap: 1rem;
		padding: 1.5rem;
		background: rgba(255, 255, 255, 0.15);
		border-radius: 16px;
		backdrop-filter: blur(10px);
		border: 1px solid rgba(255, 255, 255, 0.2);
	}

	.stat-emoji {
		font-size: 2rem;
	}

	.stat div {
		text-align: left;
	}

	.stat strong {
		display: block;
		font-size: 1.1rem;
		font-weight: 700;
		margin-bottom: 0.25rem;
	}

	.stat p {
		margin: 0;
		font-size: 0.9rem;
		opacity: 0.9;
	}

	.cta-actions {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2rem;
	}

	.action-card {
		background: white;
		border-radius: 20px;
		padding: 2.5rem;
		text-align: center;
		transition: all 0.3s ease;
	}

	.action-card:hover {
		transform: translateY(-8px);
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
	}

	.action-card h3 {
		font-size: 1.5rem;
		font-weight: 700;
		color: var(--text-primary);
		margin: 0 0 1rem 0;
	}

	.action-card p {
		font-size: 1rem;
		line-height: 1.6;
		color: var(--text-secondary);
		margin: 0 0 2rem 0;
	}

	.cta-button {
		display: inline-flex;
		align-items: center;
		gap: 0.75rem;
		padding: 1rem 2rem;
		border-radius: 12px;
		font-size: 1rem;
		font-weight: 700;
		text-decoration: none;
		transition: all 0.3s ease;
	}

	.cta-button.primary {
		background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
		color: white;
	}

	.cta-button.primary:hover {
		transform: translateX(5px);
		box-shadow: 0 10px 20px rgba(99, 102, 241, 0.3);
	}

	.cta-button.secondary {
		background: var(--text-primary);
		color: white;
	}

	.cta-button.secondary:hover {
		transform: translateX(5px);
		background: #1a1a1a;
	}

	.cta-button.tertiary {
		background: white;
		color: var(--primary-color);
		border: 2px solid var(--primary-color);
	}

	.cta-button.tertiary:hover {
		transform: translateX(5px);
		background: var(--primary-color);
		color: white;
	}

	.arrow {
		transition: transform 0.3s ease;
	}

	.cta-button:hover .arrow {
		transform: translateX(5px);
	}

	.cta-footer {
		text-align: center;
		margin-top: 4rem;
		color: white;
		position: relative;
		z-index: 1;
	}

	.cta-footer p {
		font-size: 1.1rem;
		margin: 0;
	}

	.cta-footer a {
		color: white;
		font-weight: 700;
		text-decoration: underline;
		transition: opacity 0.2s ease;
	}

	.cta-footer a:hover {
		opacity: 0.8;
	}

	@media (max-width: 1024px) {
		.cta-stats {
			grid-template-columns: 1fr;
		}

		.cta-actions {
			grid-template-columns: 1fr;
		}
	}

	@media (max-width: 768px) {
		.training-cta {
			padding: 4rem 1.5rem;
		}

		.cta-text h2 {
			font-size: 2rem;
		}

		.cta-text > p {
			font-size: 1.1rem;
		}
	}
</style>
