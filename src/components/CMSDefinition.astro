---
// CMS Definition Component - Explicación detallada de qué es un CMS
// Creado por: Eduardo
---

<section class="cms-definition">
	<div class="section-header">
		<span class="section-label">Definición</span>
		<h2>Entendiendo el concepto de <span class="highlight">CMS</span></h2>
		<p class="section-subtitle">
			Un CMS es la herramienta esencial para la gestión moderna de contenido web
		</p>
	</div>

	<div class="definition-grid">
		<div class="definition-card main-card">
			<div class="card-icon-wrapper">
				<div class="card-icon">📝</div>
			</div>
			<h3>Definición Formal</h3>
			<p>
				Un <strong>Content Management System (CMS)</strong> o Sistema de Gestión de Contenidos 
				es una aplicación de software que permite a los usuarios crear, editar, organizar y 
				publicar contenido digital sin necesidad de escribir código. Facilita la colaboración 
				entre múltiples usuarios y proporciona herramientas para gestionar todo el ciclo de 
				vida del contenido web.
			</p>
			<div class="card-tags">
				<span class="tag">Web</span>
				<span class="tag">Contenido</span>
				<span class="tag">Digital</span>
			</div>
		</div>

		<div class="definition-card">
			<div class="card-icon-wrapper small">
				<div class="card-icon">🎯</div>
			</div>
			<h3>Objetivo Principal</h3>
			<p>
				Democratizar la creación de contenido web, permitiendo a usuarios no técnicos 
				publicar y gestionar información en internet de forma autónoma, rápida y eficiente, 
				sin depender de desarrolladores.
			</p>
		</div>

		<div class="definition-card">
			<div class="card-icon-wrapper small">
				<div class="card-icon">🌐</div>
			</div>
			<h3>Accesibilidad</h3>
			<p>
				Interfaz web intuitiva accesible desde cualquier navegador, permitiendo gestionar 
				contenido desde cualquier lugar del mundo, facilitando el trabajo remoto y la 
				colaboración en tiempo real.
			</p>
		</div>

		<div class="definition-card">
			<div class="card-icon-wrapper small">
				<div class="card-icon">🔧</div>
			</div>
			<h3>Componentes Clave</h3>
			<p>
				Editor WYSIWYG, gestor de medios, sistema de plantillas, administración de usuarios, 
				gestión de permisos, SEO integrado y extensiones mediante plugins y temas.
			</p>
		</div>
	</div>

	<div class="cms-workflow">
		<h3 class="workflow-title">Flujo de trabajo típico en un CMS</h3>
		<div class="workflow-steps">
			<div class="step">
				<div class="step-number">1</div>
				<div class="step-icon">✍️</div>
				<h4>Crear</h4>
				<p>Redactar contenido usando el editor visual</p>
			</div>
			<div class="step-arrow">→</div>
			<div class="step">
				<div class="step-number">2</div>
				<div class="step-icon">🎨</div>
				<h4>Diseñar</h4>
				<p>Aplicar estilos y añadir elementos multimedia</p>
			</div>
			<div class="step-arrow">→</div>
			<div class="step">
				<div class="step-number">3</div>
				<div class="step-icon">👥</div>
				<h4>Revisar</h4>
				<p>Colaboración y aprobación del equipo</p>
			</div>
			<div class="step-arrow">→</div>
			<div class="step">
				<div class="step-number">4</div>
				<div class="step-icon">🚀</div>
				<h4>Publicar</h4>
				<p>Hacer visible el contenido al público</p>
			</div>
		</div>
	</div>

	<div class="cms-types">
		<h3 class="types-title">Tipos de CMS</h3>
		<div class="types-grid">
			<div class="type-card">
				<div class="type-header">
					<span class="type-icon">📦</span>
					<h4>CMS Tradicional</h4>
				</div>
				<p class="type-desc">
					Sistema todo-en-uno donde el contenido y la presentación están acoplados. 
					Ideal para sitios web tradicionales.
				</p>
				<div class="type-examples">
					<span class="example">WordPress</span>
					<span class="example">Drupal</span>
					<span class="example">Joomla</span>
				</div>
			</div>

			<div class="type-card">
				<div class="type-header">
					<span class="type-icon">🎯</span>
					<h4>Headless CMS</h4>
				</div>
				<p class="type-desc">
					Sistema backend que gestiona contenido y lo entrega vía API. 
					Perfecto para aplicaciones modernas y múltiples canales.
				</p>
				<div class="type-examples">
					<span class="example">Strapi</span>
					<span class="example">Contentful</span>
					<span class="example">Sanity</span>
				</div>
			</div>

			<div class="type-card">
				<div class="type-header">
					<span class="type-icon">⚡</span>
					<h4>CMS Estático</h4>
				</div>
				<p class="type-desc">
					Generador de sitios estáticos con gestión de contenido integrada. 
					Máximo rendimiento y seguridad.
				</p>
				<div class="type-examples">
					<span class="example">Hugo</span>
					<span class="example">Jekyll</span>
					<span class="example">Gatsby</span>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.cms-definition {
		padding: 6rem 2rem;
		background: linear-gradient(to bottom, #ffffff 0%, #f9fafb 100%);
	}

	.section-header {
		text-align: center;
		margin-bottom: 4rem;
		max-width: 800px;
		margin-left: auto;
		margin-right: auto;
	}

	.section-label {
		display: inline-block;
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		color: white;
		padding: 0.5rem 1.5rem;
		border-radius: 50px;
		font-weight: 600;
		font-size: 0.875rem;
		letter-spacing: 0.5px;
		margin-bottom: 1rem;
	}

	.section-header h2 {
		font-size: 2.5rem;
		font-weight: 800;
		color: var(--text-dark);
		margin-bottom: 1rem;
		line-height: 1.2;
	}

	.highlight {
		color: #667eea;
	}

	.section-subtitle {
		font-size: 1.125rem;
		color: var(--text-light);
		line-height: 1.6;
	}

	.definition-grid {
		max-width: 1200px;
		margin: 0 auto 4rem;
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 2rem;
	}

	.main-card {
		grid-column: 1 / -1;
	}

	.definition-card {
		background: white;
		padding: 2.5rem;
		border-radius: 20px;
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
		transition: all 0.3s ease;
		border: 1px solid #e5e7eb;
	}

	.definition-card:hover {
		transform: translateY(-5px);
		box-shadow: 0 10px 40px rgba(102, 126, 234, 0.15);
	}

	.card-icon-wrapper {
		width: 70px;
		height: 70px;
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		border-radius: 20px;
		display: flex;
		align-items: center;
		justify-content: center;
		margin-bottom: 1.5rem;
	}

	.card-icon-wrapper.small {
		width: 50px;
		height: 50px;
		border-radius: 14px;
	}

	.card-icon {
		font-size: 2rem;
	}

	.definition-card h3 {
		font-size: 1.5rem;
		font-weight: 700;
		color: var(--text-dark);
		margin-bottom: 1rem;
	}

	.definition-card p {
		color: var(--text-light);
		line-height: 1.8;
		margin: 0;
	}

	.card-tags {
		display: flex;
		gap: 0.75rem;
		margin-top: 1.5rem;
		flex-wrap: wrap;
	}

	.tag {
		background: #f3f4f6;
		color: #667eea;
		padding: 0.5rem 1rem;
		border-radius: 50px;
		font-size: 0.875rem;
		font-weight: 600;
	}

	.cms-workflow {
		max-width: 1200px;
		margin: 0 auto 4rem;
		background: white;
		padding: 3rem;
		border-radius: 20px;
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
	}

	.workflow-title {
		text-align: center;
		font-size: 1.75rem;
		font-weight: 700;
		color: var(--text-dark);
		margin-bottom: 2.5rem;
	}

	.workflow-steps {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 1rem;
		flex-wrap: wrap;
	}

	.step {
		flex: 1;
		min-width: 180px;
		text-align: center;
		padding: 2rem 1.5rem;
		background: #f9fafb;
		border-radius: 16px;
		position: relative;
		transition: all 0.3s ease;
	}

	.step:hover {
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		color: white;
		transform: translateY(-5px);
	}

	.step:hover .step-number,
	.step:hover .step-icon,
	.step:hover h4,
	.step:hover p {
		color: white;
	}

	.step-number {
		position: absolute;
		top: -15px;
		right: -15px;
		width: 35px;
		height: 35px;
		background: #667eea;
		color: white;
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		font-weight: 700;
		font-size: 0.9rem;
		box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
	}

	.step-icon {
		font-size: 2.5rem;
		margin-bottom: 1rem;
	}

	.step h4 {
		font-size: 1.25rem;
		font-weight: 700;
		color: var(--text-dark);
		margin-bottom: 0.5rem;
	}

	.step p {
		font-size: 0.9rem;
		color: var(--text-light);
		margin: 0;
	}

	.step-arrow {
		font-size: 2rem;
		color: #667eea;
		font-weight: 700;
	}

	.cms-types {
		max-width: 1200px;
		margin: 0 auto;
	}

	.types-title {
		text-align: center;
		font-size: 1.75rem;
		font-weight: 700;
		color: var(--text-dark);
		margin-bottom: 2.5rem;
	}

	.types-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
	}

	.type-card {
		background: white;
		padding: 2rem;
		border-radius: 20px;
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
		transition: all 0.3s ease;
		border: 2px solid transparent;
	}

	.type-card:hover {
		border-color: #667eea;
		transform: translateY(-5px);
		box-shadow: 0 10px 40px rgba(102, 126, 234, 0.15);
	}

	.type-header {
		display: flex;
		align-items: center;
		gap: 1rem;
		margin-bottom: 1rem;
	}

	.type-icon {
		font-size: 2rem;
	}

	.type-header h4 {
		font-size: 1.25rem;
		font-weight: 700;
		color: var(--text-dark);
		margin: 0;
	}

	.type-desc {
		color: var(--text-light);
		line-height: 1.7;
		margin-bottom: 1.5rem;
	}

	.type-examples {
		display: flex;
		gap: 0.75rem;
		flex-wrap: wrap;
	}

	.example {
		background: #f3f4f6;
		color: #667eea;
		padding: 0.5rem 1rem;
		border-radius: 8px;
		font-size: 0.875rem;
		font-weight: 600;
	}

	.creator-badge {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 0.75rem;
		margin-top: 4rem;
		padding: 1rem 2rem;
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		color: white;
		border-radius: 50px;
		font-weight: 600;
		width: fit-content;
		margin-left: auto;
		margin-right: auto;
		box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
	}

	.creator-icon {
		font-size: 1.5rem;
	}

	@media (max-width: 768px) {
		.cms-definition {
			padding: 4rem 1.5rem;
		}

		.section-header h2 {
			font-size: 2rem;
		}

		.definition-grid {
			grid-template-columns: 1fr;
		}

		.workflow-steps {
			flex-direction: column;
		}

		.step-arrow {
			transform: rotate(90deg);
		}

		.types-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
