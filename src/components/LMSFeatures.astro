---
// LMS Features Component - Características y beneficios de un LMS
---

<section class="lms-features">
	<div class="section-header">
		<span class="section-label">Características</span>
		<h2>¿Por qué usar un <span class="highlight">LMS</span>?</h2>
		<p class="section-subtitle">
			Descubre las ventajas que hacen del LMS una herramienta indispensable en la formación moderna
		</p>
	</div>

	<div class="features-showcase">
		<div class="feature-large">
			<div class="feature-visual">
				<div class="visual-circles">
					<div class="circle circle-1"></div>
					<div class="circle circle-2"></div>
					<div class="circle circle-3"></div>
				</div>
				<div class="visual-content">
					<div class="visual-icon">🚀</div>
					<span class="visual-label">Aprendizaje Acelerado</span>
				</div>
			</div>
			<div class="feature-info">
				<h3>Acelera el Proceso de Aprendizaje</h3>
				<p>
					Los estudiantes pueden avanzar a su propio ritmo, repetir lecciones cuando sea necesario 
					y acceder al contenido las 24 horas del día, los 7 días de la semana. Esto resulta en 
					una retención del conocimiento hasta un 60% mayor comparado con métodos tradicionales.
				</p>
				<ul class="feature-benefits">
					<li><span class="check">✓</span> Aprendizaje personalizado</li>
					<li><span class="check">✓</span> Acceso ilimitado al contenido</li>
					<li><span class="check">✓</span> Repetición sin costos adicionales</li>
				</ul>
			</div>
		</div>

		<div class="features-grid">
			<div class="feature-card">
				<div class="feature-icon">💰</div>
				<h4>Reducción de Costos</h4>
				<p>Elimina gastos de viaje, alquiler de espacios físicos, material impreso y reduce significativamente el tiempo de formación.</p>
				<div class="feature-metric">
					<span class="metric-value">-50%</span>
					<span class="metric-label">en costos de formación</span>
				</div>
			</div>

			<div class="feature-card">
				<div class="feature-icon">📊</div>
				<h4>Métricas en Tiempo Real</h4>
				<p>Dashboards completos con datos sobre progreso, participación, calificaciones y áreas de mejora para cada estudiante.</p>
				<div class="feature-metric">
					<span class="metric-value">100%</span>
					<span class="metric-label">visibilidad del progreso</span>
				</div>
			</div>

			<div class="feature-card">
				<div class="feature-icon">🎯</div>
				<h4>Personalización</h4>
				<p>Rutas de aprendizaje adaptadas a las necesidades individuales, con contenido relevante para cada perfil de usuario.</p>
				<div class="feature-metric">
					<span class="metric-value">+40%</span>
					<span class="metric-label">engagement estudiantil</span>
				</div>
			</div>

			<div class="feature-card">
				<div class="feature-icon">🔒</div>
				<h4>Consistencia del Contenido</h4>
				<p>Todos los estudiantes reciben exactamente la misma formación de calidad, eliminando variaciones entre instructores.</p>
				<div class="feature-metric">
					<span class="metric-value">100%</span>
					<span class="metric-label">uniformidad formativa</span>
				</div>
			</div>

			<div class="feature-card">
				<div class="feature-icon">🌍</div>
				<h4>Alcance Global</h4>
				<p>Forma a equipos distribuidos geográficamente sin importar las diferencias horarias o ubicaciones físicas.</p>
				<div class="feature-metric">
					<span class="metric-value">24/7</span>
					<span class="metric-label">disponibilidad global</span>
				</div>
			</div>

			<div class="feature-card">
				<div class="feature-icon">🏆</div>
				<h4>Gamificación</h4>
				<p>Insignias, puntos, rankings y certificados que motivan a los estudiantes y fomentan la competencia sana.</p>
				<div class="feature-metric">
					<span class="metric-value">+65%</span>
					<span class="metric-label">tasa de finalización</span>
				</div>
			</div>
		</div>
	</div>

	<div class="comparison-section">
		<h3 class="comparison-title">LMS vs. Formación Tradicional</h3>
		<div class="comparison-table">
			<div class="comparison-row header">
				<div class="comparison-cell">Aspecto</div>
				<div class="comparison-cell lms">LMS</div>
				<div class="comparison-cell traditional">Tradicional</div>
			</div>
			<div class="comparison-row">
				<div class="comparison-cell label">Costo por estudiante</div>
				<div class="comparison-cell lms"><span class="badge positive">Bajo</span></div>
				<div class="comparison-cell traditional"><span class="badge negative">Alto</span></div>
			</div>
			<div class="comparison-row">
				<div class="comparison-cell label">Escalabilidad</div>
				<div class="comparison-cell lms"><span class="badge positive">Ilimitada</span></div>
				<div class="comparison-cell traditional"><span class="badge negative">Limitada</span></div>
			</div>
			<div class="comparison-row">
				<div class="comparison-cell label">Flexibilidad horaria</div>
				<div class="comparison-cell lms"><span class="badge positive">24/7</span></div>
				<div class="comparison-cell traditional"><span class="badge negative">Fija</span></div>
			</div>
			<div class="comparison-row">
				<div class="comparison-cell label">Seguimiento detallado</div>
				<div class="comparison-cell lms"><span class="badge positive">Automático</span></div>
				<div class="comparison-cell traditional"><span class="badge negative">Manual</span></div>
			</div>
			<div class="comparison-row">
				<div class="comparison-cell label">Actualización de contenido</div>
				<div class="comparison-cell lms"><span class="badge positive">Instantánea</span></div>
				<div class="comparison-cell traditional"><span class="badge negative">Lenta</span></div>
			</div>
		</div>
	</div>
</section>

<style>
	.lms-features {
		padding: 6rem 2rem;
		max-width: 1400px;
		margin: 0 auto;
		background: var(--bg-secondary);
	}

	.section-header {
		text-align: center;
		margin-bottom: 4rem;
	}

	.section-label {
		display: inline-block;
		font-size: 0.85rem;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 2px;
		color: var(--primary-color);
		margin-bottom: 1rem;
	}

	.section-header h2 {
		font-size: 2.75rem;
		font-weight: 800;
		color: var(--text-primary);
		margin: 0 0 1rem 0;
	}

	.highlight {
		position: relative;
		color: var(--primary-color);
	}

	.section-subtitle {
		font-size: 1.2rem;
		color: var(--text-secondary);
		max-width: 600px;
		margin: 0 auto;
	}

	.features-showcase {
		margin-bottom: 5rem;
	}

	.feature-large {
		display: grid;
		grid-template-columns: 1fr 1.2fr;
		gap: 3rem;
		background: var(--surface-color);
		border-radius: 24px;
		padding: 3rem;
		margin-bottom: 2rem;
		border: 1px solid var(--border-color);
		align-items: center;
	}

	.feature-visual {
		position: relative;
		height: 300px;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.visual-circles {
		position: absolute;
		width: 100%;
		height: 100%;
	}

	.circle {
		position: absolute;
		border-radius: 50%;
		animation: pulse 3s ease-in-out infinite;
	}

	.circle-1 {
		width: 200px;
		height: 200px;
		background: linear-gradient(135deg, rgba(99, 102, 241, 0.2), rgba(139, 92, 246, 0.1));
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
	}

	.circle-2 {
		width: 250px;
		height: 250px;
		border: 2px solid rgba(99, 102, 241, 0.2);
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
		animation-delay: 0.5s;
	}

	.circle-3 {
		width: 300px;
		height: 300px;
		border: 1px dashed rgba(99, 102, 241, 0.15);
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
		animation-delay: 1s;
	}

	@keyframes pulse {
		0%, 100% { opacity: 1; transform: translate(-50%, -50%) scale(1); }
		50% { opacity: 0.7; transform: translate(-50%, -50%) scale(1.05); }
	}

	.visual-content {
		position: relative;
		z-index: 1;
		text-align: center;
	}

	.visual-icon {
		font-size: 4rem;
		margin-bottom: 1rem;
		display: block;
	}

	.visual-label {
		font-weight: 700;
		color: var(--primary-color);
		font-size: 1.1rem;
	}

	.feature-info h3 {
		font-size: 1.75rem;
		font-weight: 700;
		color: var(--text-primary);
		margin: 0 0 1rem 0;
	}

	.feature-info p {
		font-size: 1.1rem;
		line-height: 1.8;
		color: var(--text-secondary);
		margin: 0 0 1.5rem 0;
	}

	.feature-benefits {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}

	.feature-benefits li {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		font-weight: 500;
		color: var(--text-primary);
	}

	.check {
		color: #10b981;
		font-weight: 700;
		font-size: 1.1rem;
	}

	.features-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1.5rem;
	}

	.feature-card {
		background: var(--surface-color);
		border-radius: 20px;
		padding: 2rem;
		border: 1px solid var(--border-color);
		transition: all 0.3s ease;
	}

	.feature-card:hover {
		transform: translateY(-5px);
		box-shadow: var(--shadow-lg);
		border-color: var(--primary-color);
	}

	.feature-icon {
		font-size: 2.5rem;
		margin-bottom: 1rem;
	}

	.feature-card h4 {
		font-size: 1.25rem;
		font-weight: 700;
		color: var(--text-primary);
		margin: 0 0 0.75rem 0;
	}

	.feature-card p {
		font-size: 0.95rem;
		line-height: 1.6;
		color: var(--text-secondary);
		margin: 0 0 1.5rem 0;
	}

	.feature-metric {
		display: flex;
		align-items: baseline;
		gap: 0.5rem;
		padding-top: 1rem;
		border-top: 1px solid var(--border-color);
	}

	.metric-value {
		font-size: 1.5rem;
		font-weight: 800;
		background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.metric-label {
		font-size: 0.85rem;
		color: var(--text-muted);
	}

	.comparison-section {
		background: var(--surface-color);
		border-radius: 24px;
		padding: 3rem;
		border: 1px solid var(--border-color);
	}

	.comparison-title {
		font-size: 1.75rem;
		font-weight: 700;
		color: var(--text-primary);
		margin: 0 0 2rem 0;
		text-align: center;
	}

	.comparison-table {
		border-radius: 16px;
		overflow: hidden;
		border: 1px solid var(--border-color);
	}

	.comparison-row {
		display: grid;
		grid-template-columns: 1.5fr 1fr 1fr;
	}

	.comparison-row.header {
		background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
	}

	.comparison-row.header .comparison-cell {
		color: white;
		font-weight: 700;
		font-size: 1rem;
	}

	.comparison-row:not(.header) {
		background: var(--bg-secondary);
	}

	.comparison-row:not(.header):nth-child(even) {
		background: var(--surface-color);
	}

	.comparison-cell {
		padding: 1rem 1.5rem;
		display: flex;
		align-items: center;
	}

	.comparison-cell.label {
		font-weight: 600;
		color: var(--text-primary);
	}

	.comparison-cell.lms,
	.comparison-cell.traditional {
		justify-content: center;
	}

	.badge {
		padding: 0.4rem 1rem;
		border-radius: 50px;
		font-size: 0.85rem;
		font-weight: 600;
	}

	.badge.positive {
		background: rgba(16, 185, 129, 0.1);
		color: #10b981;
		border: 1px solid rgba(16, 185, 129, 0.2);
	}

	.badge.negative {
		background: rgba(239, 68, 68, 0.1);
		color: #ef4444;
		border: 1px solid rgba(239, 68, 68, 0.2);
	}

	@media (max-width: 1024px) {
		.feature-large {
			grid-template-columns: 1fr;
		}

		.feature-visual {
			height: 200px;
		}

		.features-grid {
			grid-template-columns: repeat(2, 1fr);
		}
	}

	@media (max-width: 768px) {
		.lms-features {
			padding: 4rem 1.5rem;
		}

		.section-header h2 {
			font-size: 2rem;
		}

		.features-grid {
			grid-template-columns: 1fr;
		}

		.comparison-row {
			grid-template-columns: 1fr;
			gap: 0;
		}

		.comparison-cell {
			justify-content: flex-start !important;
			padding: 0.75rem 1rem;
		}

		.comparison-row.header {
			display: none;
		}

		.comparison-cell.label {
			background: linear-gradient(135deg, rgba(99, 102, 241, 0.1), rgba(139, 92, 246, 0.1));
			border-bottom: none;
		}

		.comparison-cell.lms::before {
			content: 'LMS: ';
			font-weight: 600;
			margin-right: 0.5rem;
		}

		.comparison-cell.traditional::before {
			content: 'Tradicional: ';
			font-weight: 600;
			margin-right: 0.5rem;
		}
	}
</style>
