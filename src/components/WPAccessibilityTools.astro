<section class="tools">
    <div class="container">
        <h2 class="section-title">Herramientas para comprobar accesibilidad</h2>
        <p class="section-subtitle">
            Con estas revisiones básicas detectas los fallos más típicos sin ser experto.
        </p>

        <div class="grid">
            <article class="tool">
                <div class="icon">⌨️</div>
                <h3>Prueba de teclado</h3>
                <p>Usa TAB / Shift+TAB: ¿llegas a todo? ¿se ve el foco? ¿te quedas “atrapado”?</p>
            </article>

            <article class="tool">
                <div class="icon">🧪</div>
                <h3>Lighthouse (Chrome)</h3>
                <p>Informe rápido con errores típicos: contraste, labels, estructura, etc.</p>
            </article>

            <article class="tool">
                <div class="icon">🌊</div>
                <h3>WAVE (extensión)</h3>
                <p>Marca problemas sobre la página: alt, formularios, encabezados, avisos.</p>
            </article>

            <article class="tool">
                <div class="icon">🎯</div>
                <h3>Contraste</h3>
                <p>Si necesitas entrecerrar los ojos… el contraste no está haciendo su trabajo.</p>
            </article>
        </div>
    </div>
</section>

<style>
    .tools {
        padding: 3rem 0 5rem 0;
    }

    .section-title {
        font-size: 2.2rem;
        font-weight: 900;
        margin: 0 0 0.75rem 0;
        text-align: center;
    }

    .section-subtitle {
        max-width: 820px;
        margin: 0 auto 2.5rem auto;
        text-align: center;
        color: var(--text-secondary);
        font-size: 1.05rem;
    }

    .grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 1.5rem;
    }

    .tool {
        background: var(--surface-color);
        border: 1px solid var(--border-color);
        border-radius: var(--radius-xl);
        padding: 2rem;
        box-shadow: var(--shadow-sm);
        transition: transform var(--transition-base), box-shadow var(--transition-base);
    }

    .tool:hover {
        transform: translateY(-6px);
        box-shadow: var(--shadow-lg);
    }

    .icon {
        width: 52px;
        height: 52px;
        border-radius: 14px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, rgba(139, 92, 246, 0.18), rgba(6, 182, 212, 0.18));
        font-size: 1.5rem;
        margin-bottom: 1rem;
    }

    .tool h3 {
        margin: 0 0 0.75rem 0;
        font-size: 1.25rem;
        font-weight: 800;
    }

    .tool p {
        margin: 0;
        color: var(--text-secondary);
        line-height: 1.75;
    }

    @media (max-width: 900px) {
        .grid {
            grid-template-columns: 1fr;
        }
    }
</style>
