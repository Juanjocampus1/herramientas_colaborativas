---
import Layout from '../layouts/Layout.astro';
import LMSHero from '../components/LMSHero.astro';
import LMSDefinition from '../components/LMSDefinition.astro';
import LMSFeatures from '../components/LMSFeatures.astro';
---

<Layout>
	<main class="lms-page">
		<LMSHero />
		<LMSDefinition />
		<LMSFeatures />

		<section class="lms-examples">
			<div class="section-header">
				<span class="section-label">Ejemplos</span>
				<h2>Plataformas <span class="highlight">LMS</span> Populares</h2>
				<p class="section-subtitle">
					Conoce las herramientas más utilizadas en el mercado educativo y empresarial
				</p>
			</div>

			<div class="platforms-grid">
				<div class="platform-card">
					<div class="platform-logo">
						<span class="logo-emoji">🎓</span>
					</div>
					<h3>Moodle</h3>
					<p class="platform-type">Open Source</p>
					<p class="platform-desc">
						El LMS de código abierto más popular del mundo, utilizado por universidades 
						y empresas de todos los tamaños.
					</p>
					<ul class="platform-features">
						<li>Gratuito y personalizable</li>
						<li>Gran comunidad de soporte</li>
						<li>Múltiples plugins disponibles</li>
					</ul>
				</div>

				<div class="platform-card featured">
					<div class="featured-badge">Popular</div>
					<div class="platform-logo">
						<span class="logo-emoji">📚</span>
					</div>
					<h3>Canvas LMS</h3>
					<p class="platform-type">Comercial / Open Source</p>
					<p class="platform-desc">
						Plataforma moderna con interfaz intuitiva, muy popular en instituciones 
						educativas de EE.UU. y Europa.
					</p>
					<ul class="platform-features">
						<li>Interfaz moderna y limpia</li>
						<li>Integración con herramientas</li>
						<li>App móvil robusta</li>
					</ul>
				</div>

				<div class="platform-card">
					<div class="platform-logo">
						<span class="logo-emoji">🏢</span>
					</div>
					<h3>Blackboard</h3>
					<p class="platform-type">Comercial</p>
					<p class="platform-desc">
						Solución empresarial completa con funcionalidades avanzadas para 
						grandes organizaciones educativas.
					</p>
					<ul class="platform-features">
						<li>Solución enterprise robusta</li>
						<li>Análisis avanzados</li>
						<li>Soporte profesional</li>
					</ul>
				</div>

				<div class="platform-card">
					<div class="platform-logo">
						<span class="logo-emoji">💼</span>
					</div>
					<h3>TalentLMS</h3>
					<p class="platform-type">Comercial / SaaS</p>
					<p class="platform-desc">
						LMS en la nube ideal para formación corporativa, fácil de configurar 
						y con precios accesibles.
					</p>
					<ul class="platform-features">
						<li>Configuración rápida</li>
						<li>Gamificación integrada</li>
						<li>Ideal para empresas</li>
					</ul>
				</div>

				<div class="platform-card">
					<div class="platform-logo">
						<span class="logo-emoji">🚀</span>
					</div>
					<h3>Teachable</h3>
					<p class="platform-type">Comercial / SaaS</p>
					<p class="platform-desc">
						Plataforma perfecta para creadores de cursos independientes que quieren 
						vender su conocimiento online.
					</p>
					<ul class="platform-features">
						<li>Monetización integrada</li>
						<li>Sin conocimientos técnicos</li>
						<li>Marketing incluido</li>
					</ul>
				</div>

				<div class="platform-card">
					<div class="platform-logo">
						<span class="logo-emoji">🎯</span>
					</div>
					<h3>LearnDash</h3>
					<p class="platform-type">Plugin WordPress</p>
					<p class="platform-desc">
						El plugin LMS más potente para WordPress, convierte cualquier sitio 
						en una plataforma de cursos completa.
					</p>
					<ul class="platform-features">
						<li>Integración con WordPress</li>
						<li>Drip content</li>
						<li>Certificados automáticos</li>
					</ul>
				</div>
			</div>
		</section>

		<section class="cta-section">
			<div class="cta-content">
				<h2>¿Listo para implementar un LMS?</h2>
				<p>
					Un LMS bien implementado puede transformar completamente la forma en que 
					tu organización aborda la formación y el desarrollo de talento.
				</p>
				<div class="cta-buttons">
					<a href="/" class="btn btn-primary">Volver al Inicio</a>
					<a href="/seguridad-wordpress" class="btn btn-secondary">Ver Seguridad WordPress</a>
				</div>
			</div>
		</section>
	</main>
</Layout>

<style>
	.lms-page {
		background: var(--bg-primary);
		min-height: 100vh;
	}

	.section-header {
		text-align: center;
		margin-bottom: 4rem;
	}

	.section-label {
		display: inline-block;
		font-size: 0.85rem;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 2px;
		color: var(--primary-color);
		margin-bottom: 1rem;
	}

	.section-header h2 {
		font-size: 2.75rem;
		font-weight: 800;
		color: var(--text-primary);
		margin: 0 0 1rem 0;
	}

	.highlight {
		position: relative;
		color: var(--primary-color);
	}

	.section-subtitle {
		font-size: 1.2rem;
		color: var(--text-secondary);
		max-width: 600px;
		margin: 0 auto;
	}

	.lms-examples {
		padding: 6rem 2rem;
		max-width: 1400px;
		margin: 0 auto;
	}

	.platforms-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2rem;
	}

	.platform-card {
		background: var(--surface-color);
		border-radius: 20px;
		padding: 2rem;
		border: 1px solid var(--border-color);
		transition: all 0.3s ease;
		position: relative;
	}

	.platform-card:hover {
		transform: translateY(-5px);
		box-shadow: var(--shadow-lg);
		border-color: var(--primary-color);
	}

	.platform-card.featured {
		border-color: var(--primary-color);
		background: linear-gradient(135deg, var(--surface-color), rgba(99, 102, 241, 0.05));
	}

	.featured-badge {
		position: absolute;
		top: -10px;
		right: 20px;
		background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
		color: white;
		padding: 0.4rem 1rem;
		border-radius: 50px;
		font-size: 0.8rem;
		font-weight: 700;
	}

	.platform-logo {
		width: 70px;
		height: 70px;
		border-radius: 16px;
		background: linear-gradient(135deg, rgba(99, 102, 241, 0.1), rgba(139, 92, 246, 0.1));
		display: flex;
		align-items: center;
		justify-content: center;
		margin-bottom: 1.5rem;
	}

	.logo-emoji {
		font-size: 2rem;
	}

	.platform-card h3 {
		font-size: 1.5rem;
		font-weight: 700;
		color: var(--text-primary);
		margin: 0 0 0.5rem 0;
	}

	.platform-type {
		font-size: 0.85rem;
		font-weight: 600;
		color: var(--primary-color);
		margin: 0 0 1rem 0;
	}

	.platform-desc {
		font-size: 0.95rem;
		line-height: 1.6;
		color: var(--text-secondary);
		margin: 0 0 1.5rem 0;
	}

	.platform-features {
		list-style: none;
		padding: 0;
		margin: 0;
		border-top: 1px solid var(--border-color);
		padding-top: 1.5rem;
	}

	.platform-features li {
		padding: 0.5rem 0;
		font-size: 0.9rem;
		color: var(--text-secondary);
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	.platform-features li::before {
		content: '✓';
		color: #10b981;
		font-weight: 700;
	}

	.cta-section {
		padding: 6rem 2rem;
		background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
	}

	.cta-content {
		max-width: 800px;
		margin: 0 auto;
		text-align: center;
		color: white;
	}

	.cta-content h2 {
		font-size: 2.5rem;
		font-weight: 800;
		margin: 0 0 1rem 0;
	}

	.cta-content p {
		font-size: 1.2rem;
		line-height: 1.8;
		opacity: 0.95;
		margin: 0 0 2rem 0;
	}

	.cta-buttons {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	.btn {
		padding: 1rem 2rem;
		border-radius: 50px;
		text-decoration: none;
		font-weight: 600;
		font-size: 1rem;
		transition: all 0.3s ease;
		display: inline-block;
	}

	.btn-primary {
		background: white;
		color: var(--primary-color);
	}

	.btn-primary:hover {
		transform: translateY(-3px);
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
	}

	.btn-secondary {
		background: transparent;
		color: white;
		border: 2px solid white;
	}

	.btn-secondary:hover {
		background: white;
		color: var(--primary-color);
		transform: translateY(-3px);
	}

	@media (max-width: 1024px) {
		.platforms-grid {
			grid-template-columns: repeat(2, 1fr);
		}
	}

	@media (max-width: 768px) {
		.lms-examples {
			padding: 4rem 1.5rem;
		}

		.section-header h2 {
			font-size: 2rem;
		}

		.platforms-grid {
			grid-template-columns: 1fr;
		}

		.cta-content h2 {
			font-size: 1.75rem;
		}

		.cta-buttons {
			flex-direction: column;
			align-items: center;
		}

		.btn {
			width: 100%;
			max-width: 300px;
			text-align: center;
		}
	}
</style>
